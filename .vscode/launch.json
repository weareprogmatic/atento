{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug atento",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/atento-cli",
            "args": [
                //"--version",
                "${workspaceFolder}/workflows/_single.yaml"
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build",
            "stopOnEntry": false,
            "sourceLanguages": [
                "rust"
            ],
            "console": "integratedTerminal"
        }
    ]
}